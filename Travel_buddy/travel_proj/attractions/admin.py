from django.contrib import admin
from .models import Attraction # Import the Attraction model

admin.site.register(Attraction)  # Register the Attraction model with the admin site

# Register your models here.
